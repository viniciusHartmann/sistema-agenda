{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "concurrently --names \"php,vite\" --prefix-colors \"blue,green\" \"php artisan serve --host=127.0.0.1 --port=8000\" \"vite\""
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.1",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    },
    "dependencies": {
        "axios": "^1.16.1",
        "vue-router": "^5.0.7"
    }
}
